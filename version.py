"""Application version — single source of truth.

Update together on every release:
  - this file (__version__, shown in UI notice bar via /api/ping)
  - buildozer.spec (version=..., Android package version)
"""

__version__ = "2.6.2"
